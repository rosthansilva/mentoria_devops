c = float(input("Qual a temperatura atual em °C? "))
f = ((c * 9) / 5) + 32  # formula para conversão de temperatura °C em °F
print('A temperatura °C {} seria aproximadamente em °F de {}'.format(c, f))
