import math # comando usado para importar a biblioteca math inteira
num = int(input('Digite um numero: '))
raiz = math.sqrt(num)
print('A raiz quadrada de {} é {}'.format(num,  math.floor(raiz)))
